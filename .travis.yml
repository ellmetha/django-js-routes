dist: xenial

language: python

addons:
  apt:
    sources:
      - travis-ci/sqlite3
    packages:
      - sqlite3

python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8-dev"

env:
  - DJANGO_ENV="django>=2.0,<2.1"
  - DJANGO_ENV="django>=2.1,<2.2"
  - DJANGO_ENV="django>=2.2,<3.0"
  - DJANGO_ENV="django==3.0b1"

matrix:
  exclude:
    - python: "3.4"
      env: DJANGO_ENV="django>=2.1,<2.2"
    - python: "3.4"
      env: DJANGO_ENV="django>=2.2,<3.0"
    - python: "3.4"
      env: DJANGO_ENV="django==3.0b1"
    - python: "3.5"
      env: DJANGO_ENV="django==3.0b1"

install:
  - pip install pip pipenv -U
  - pipenv install "$DJANGO_ENV" --skip-lock
  - pipenv install --dev --skip-lock
  - npm install --dev
script:
  - make qa
  - make coverage
after_success:
  - codecov
branches:
  only:
    - master
