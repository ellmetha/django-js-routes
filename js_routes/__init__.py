"""
    Django JS Routes
    ================

    Django JS Routes is a Django application allowing to expose and perform reverse lookups of
    Django URLs in the frontend world. It enables the ability to configure which Django URL should
    be exposed in a Javascript export helper and comes with a compiled helper that allows to perform
    reverse URL lookups on the client side.

"""


default_app_config = 'js_routes.apps.JSRoutesAppConfig'
